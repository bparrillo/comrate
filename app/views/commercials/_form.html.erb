<%= form_for(commercial, html: {multipart: true}) do |f| %>
  <% if commercial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commercial.errors.count, "error") %> prohibited this commercial from being saved:</h2>

      <ul>
      <% commercial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_for @commercial, html: {multipart: true} do |f| %>
    <%= hidden_field_tag 'user', @user %>
    <p>
      <%= f.label :title %> 
      <%= f.text_field :title %>
    </p>
    <p>
      <%= f.label :description %>
      <%= f.text_area :description %>
    </p>
    <p>
      <%= f.label :video %>
      <%= f.file_field :video, class: "file-selector" %>
    </p>
    <p>
      <%= f.submit %>
    </p>
  <% end %>
<% end %>

<%= link_to "back to listing", commercials_path %>

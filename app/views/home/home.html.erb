<%= !!current_user ? render(partial: "shared/navbar") : "" %>
<h1 align="center">Welcome to Comrate!</h1>
<body>
  <%= !current_user ? link_to('Sign Up', new_user_registration_path) : "" %>
  <%= !current_user ? link_to('Login', new_user_session_path) : "" %>
</body>

